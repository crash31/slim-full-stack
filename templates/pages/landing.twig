{% extends 'layout.twig' %}

{% block body %}

{{ parent() }}

{% if name %}
  <h2>Hello {{ name }}!</h2>
{% else %}
  <p class="lead">Good to see you.</p>
  <p><small>If you are seeing this, that means things are running smoothely.</small></p>
{% endif %}

{% endblock %}
